# Upcoming

- Expanded `brain-style` with server route guidance that steers agents toward RESTful, resource-oriented URLs and away from action-style endpoints when a standard HTTP method and resource path fits.
