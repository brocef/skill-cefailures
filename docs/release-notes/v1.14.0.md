# Upcoming

- Capability documentation and multi-repository inbox workflows now adapt to each project's own structure. They no longer assume specific repository names, application types, branch policies, coordination tools, or a shared product layer.
