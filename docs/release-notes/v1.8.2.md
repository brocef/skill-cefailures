# v1.8.2

- Decoupled the capabilities-SDLC skill from any specific inter-agent transport. The product-layer coordination protocol now describes the contract between per-repo agents and the orchestrator only — the transport (broker, Claude teams, or anything else) is named in the host project's instructions instead.
