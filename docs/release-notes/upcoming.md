# Upcoming

## Clearer version-cut choices

The `documentation-sync` skill now presents a concrete five-option choice list after a major completed change set: micro, minor, or major version bump; update only the `upcoming.md` docs; or skip both the version bump and documentation updates.
