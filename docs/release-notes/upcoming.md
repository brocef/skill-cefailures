# Upcoming

- Added Codex compatibility for local skill installs. `install_skill.py` can now target Claude Code, Codex, or both.
- Renamed the repo instruction file to `AGENTS.md` while keeping `CLAUDE.md` as a compatibility symlink.
