# Upcoming

## `documentation-sync` skill split, plus a new `/cut-version` command

The `documentation-sync` skill's SKILL.md is now 73% smaller (333 → 91 lines). Three deep workflows that only matter in specific situations have moved out of the auto-loaded routing layer:

- **Release-notes and changelog work** — entry format, hash wrappers, release-notes-vs-changelog tone, version cross-check before append, and existing-`CHANGELOG.md` migration suggestions — now live in `docs/release-notes-and-changelogs.md` inside the skill. Loaded only when an agent is actually writing into release-notes or changelog files.
- **Follow-ups (`docs/FOLLOWUPS.md`)** — entry format, what-counts/what-doesn't, lifecycle, recommended doc-sync entry — now live in `docs/follow-ups.md` inside the skill. Loaded only when an agent is prepending or annotating a follow-up entry.
- **Cutting a version** — the bump-rotate-commit-tag recipe (including bump-size guidance and version-cut common mistakes) — is now `/skill-cefailures:documentation-sync:cut-version`. The trigger to *offer* a version cut after a coherent change set stays in the skill's SKILL.md.
