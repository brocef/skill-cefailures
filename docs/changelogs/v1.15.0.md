# Upcoming

<changes starting-hash="8f64028" ending-hash="be2103c">
- Added `skills/report/SKILL.md` — routes plugin feedback to https://github.com/brocef/skill-cefailures/issues with bug and suggestion skeletons. Modeled on the TCW plugin's `tcw-report` skill, but with frontmatter pared to this repo's `name` + `description` convention (no sibling skill uses `allowed-tools`/`when_to_use`/`metadata`); adapted for this plugin's surfaces (skill / slash command / broker / `create_skill.py`), its dual Claude Code + Codex targeting, and its version source. Registered in `README.md` (tree + skill table), `AGENTS.md`, and the `keywords` array of both `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`.
- Retired `commands/process-inbox.md` and `commands/process-inbox-initiative.md` outright, and removed `docs/inbox/` (including the processed `.archive/proposal.md`, recoverable from git history). The `docs/inbox/` request-document convention is superseded by the TCW work axis (`tcw work delegate` / `tcw work escalate` → `docs/work/inbox/` triage); both commands were auto-offering themselves in agents' skill lists for a workflow whose folders no longer exist.
- Added volatility as a first-class routing axis to `commands/brain-style/claude-md.md`, per the `2026-06-16-claude-md-anti-volatility-rule` inbox request: a new **Route when** bullet for facts that change on an ordinary development cadence, a new **Keep It Stable (Anti-Volatility)** section carrying the "would a routine PR make this line wrong?" test plus a banned-content table (exact dep pins, Status sections, deep file paths, long operational blocks), and three matching **Common Mistakes** rows.
- Moved `docs/inbox/2026-06-17-claude-code-markdown-web-fetch-hook.md` to `docs/plans/` — an unimplemented design spec for a `PreToolUse` WebFetch hook preferring Markdown representations, kept whole rather than compressed into a follow-up bullet, and tracked in `docs/FOLLOWUPS.md`.
- Dropped the `process-inbox` command-tree entries, slash-command table rows, and skill-list mentions from `README.md` and `AGENTS.md` (symlinked as `CLAUDE.md`).
- Added `requirements-dev.txt` declaring the previously-undeclared test dependencies. `requirements.txt` carried only the three runtime deps, so the documented workflow (`pip install -r requirements.txt` → `python -m pytest tests/ -v`) could not reach a green suite on a fresh checkout: `pytest-asyncio` was missing, leaving the 8 async broker tests in `test_broker_client.py` / `test_broker_transport.py` unrunnable ("async def functions are not natively supported"), and 2 `test_create_skill.py` tests import `openai`, which is deliberately optional at runtime but required by the suite. `pytest` itself was also undeclared. Suite goes 201 passed / 10 failed → 211 passed. Documented in `AGENTS.md` Common Commands and the `README.md` file tree.
</changes>

## Not done

- `docs/plans/2026-07-13-remove-proposit-dependencies.md` still references `process-inbox-initiative` and `docs/inbox/`. Left as a dated planning record; several of its tasks are now moot. Tracked in `docs/FOLLOWUPS.md`.
