# Upcoming

<changes starting-hash="2b074d0" ending-hash="HEAD">
- `commands/brain-style/review.md`: new file. `/skill-cefailures:brain-style:review` — lifts the prior `skills/brain-style/docs/brain-review.md` content into a command, framed for explicit user invocation.
- `commands/brain-style/claude-md.md`: new file. `/skill-cefailures:brain-style:claude-md` — lifts the prior `skills/brain-style/docs/claude-md.md` content; cross-link to the TypeScript sub-style now points at `skills/brain-style/docs/typescript.md`; cross-link to documentation-sync setup now points at the new command.
- `commands/documentation-sync/setup.md`: new file. `/skill-cefailures:documentation-sync:setup` — lifts the prior `skills/documentation-sync/docs/setup.md`; trigger-reference cross-link now names the documentation-sync SKILL.md explicitly.
- `commands/permissions-auditor/install.md`: new file. `/skill-cefailures:permissions-auditor:install` — lifts the prior install subskill; "two directories up from this file" path-up math updated for the new location.
- `commands/permissions-auditor/analyze.md`: new file. `/skill-cefailures:permissions-auditor:analyze` — lifts the prior analyze subskill; path-up math updated; cross-link to install now points at the install command.
- `skills/brain-style/SKILL.md`: trimmed description (removed brain-review and claude-md trigger clauses); routing table now lists only `docs/typescript.md`; added a "Companion commands" section pointing to the two new commands.
- `skills/brain-style/docs/brain-review.md`, `skills/brain-style/docs/claude-md.md`: deleted; content migrated to commands.
- `skills/documentation-sync/SKILL.md`: removed the trailing `## Subskills` table; rewrote the "If the section is missing, ask the user…" sentence to point at the new `:setup` command.
- `skills/documentation-sync/docs/setup.md`: deleted; content migrated to a command. The now-empty `docs/` directory under documentation-sync was also removed.
- `skills/permissions-auditor/`: deleted entirely (SKILL.md + both docs). The skill's two user-explicit workflows are now the two commands above.
- `.claude-plugin/plugin.json`: `permissions-auditor` dropped from the `keywords` array.
- `README.md`: skills table no longer lists `permissions-auditor`; added the missing `capabilities-sdlc` row (pre-existing inconsistency); slash-commands table now lists the five new commands plus the explanatory framing for why commands and skills exist for different jobs; repo-structure tree updated to show command subdirectories and the corrected skill count.
- `commands/broker/setup.md`: new file. `/skill-cefailures:broker:setup` — thin pointer instructing the agent to read `skills/broker/docs/setup.md` and walk the user through first-time install (CLI symlink, server, identity, allowedTools).
- `commands/broker/mode.md`: new file. `/skill-cefailures:broker:mode` — replaces the old `/broker-mode`. Tells the agent to read `patterns.md`'s Broker Mode section plus the mandatory `critical-rules.md`, `authority.md`, `signals.md`.
- `commands/broker/send.md`: new file. `/skill-cefailures:broker:send` — references `usage.md`, `signals.md`, `critical-rules.md`. Accepts `$ARGUMENTS` as message intent.
- `commands/broker/read.md`: new file. `/skill-cefailures:broker:read` — references `usage.md`'s `broker recv` and `broker history` sections; describes the cursor-advancing vs. peek distinction.
- `commands/broker/doctor.md`: new file. `/skill-cefailures:broker:doctor` — references `health-check.md`; reports findings as a ✓/✗ checklist.
- `commands/broker-mode.md`: deleted. Replaced by `commands/broker/mode.md`.
- `skills/broker/SKILL.md`: rewritten to a namespace stub (frontmatter + a 2-sentence body explaining that the file exists only to host the docs/ tree). Description now lists all five commands and explicitly says "do not auto-load this skill". The 7-row routing table is gone.
- `skills/broker/docs/*.md`: untouched. All seven reference docs (`setup`, `usage`, `patterns`, `critical-rules`, `signals`, `authority`, `troubleshooting`, `health-check`) remain in place and are read by commands via path.
- `README.md`: broker skill row updated; `/broker-mode` row replaced with the five new broker commands; quick-start section now points at `/skill-cefailures:broker:setup` and `:doctor`; agent-side description swapped from "load the skill" to "run the command"; remaining `/broker-mode` references replaced with `/skill-cefailures:broker:mode`; repo-structure tree now lists `commands/broker/` instead of `commands/broker-mode.md`.
- `docs/release-notes/upcoming.md`, `docs/changelogs/upcoming.md`: extended with the wave-2 broker conversion.
- `docs/FOLLOWUPS.md`: broker conversion item struck through (completed 2026-05-19); documentation-sync deeper refactor remains as the open item.
</changes>
