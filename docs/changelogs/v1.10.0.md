# Upcoming

## Added

- `commands/process-inbox.md` — new top-level slash command `/skill-cefailures:process-inbox`. Enumerates loose `.md`/`.txt` files and folder bundles in `docs/inbox/` (ignoring `.archive/` and hidden entries), auto-selects the only candidate or asks if there are multiple, resolves the request doc inside a bundle via conventional names (`request.{md,txt}` → `proposal.{md,txt}` → `README.md`) before falling back to ask, routes via `superpowers:*` skills when present, and moves the source into `docs/inbox/.archive/` after ingestion (timestamp-suffixed on name collision).
- `README.md` — registered the new command in the Slash Commands table and the repo-structure tree.
