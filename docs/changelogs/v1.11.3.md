# v1.11.3

## Added

<changes starting-hash="e93e735" ending-hash="HEAD">
- Added `.codex-plugin/plugin.json` with Codex plugin metadata for the existing skills.
- Added `.agents/plugins/marketplace.json` and `plugins/skill-cefailures -> ..` so Codex can discover `skill-cefailures` as a marketplace plugin using the repo marketplace layout.
- Updated `README.md` with Codex plugin installation instructions and documented the Codex plugin metadata files.
- Updated `AGENTS.md` version-cut guidance to keep Claude and Codex plugin metadata versions in sync and require changes to remain compatible with both agent surfaces unless explicitly scoped otherwise.
</changes>
