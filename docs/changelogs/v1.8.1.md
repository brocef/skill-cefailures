# v1.8.1

- `AGENTS.md`: renamed the repo-level instruction file from `CLAUDE.md`; `CLAUDE.md` is now a symlink for Claude Code compatibility.
- `scripts/install_skill.py`: added `--agent {claude,codex,all}` so symlink installs can target `~/.claude/skills/`, `~/.codex/skills/`, or both.
- `tests/test_install_skill.py`: added coverage for Codex-only and all-agent installation targets.
- `README.md`: documented Codex-compatible local installation and the `AGENTS.md`/`CLAUDE.md` compatibility layout.
<changes starting-hash="5b49aa3" ending-hash="5b49aa3">
- `skills/broker/SKILL.md`: changed the skill description to explicit-invocation-only so broker does not auto-load on generic collaboration, inbox, identity, or broker-command mentions.
- `commands/broker-mode.md`: clarified that Broker Mode is entered explicitly.
- `README.md`: documented broker as opt-in and updated the agent usage guidance accordingly.
- `docs/release-notes/upcoming.md`: added a user-facing note about explicit broker invocation.
</changes>
