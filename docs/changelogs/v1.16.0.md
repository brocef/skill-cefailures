# Upcoming

- Removed six skills and all supporting infrastructure: `broker`, `elkjs`, `ieee`, `knex`, `openai-api`, `typebox`.
  - Deleted `skills/{broker,elkjs,ieee,knex,openai-api,typebox}/`.
  - Deleted the broker CLI/server (`scripts/broker_*.py`), its tests (`tests/test_broker_*.py`), and its slash commands (`commands/broker/`).
  - Dropped `mcp` from `requirements.txt` (only the broker used it) and the `mcp_broker` block from `data/recommended-permissions.json`.
  - Pruned the removed skills from `plugin.json` keywords (both surfaces) and the Codex interface text; removed the Message Broker section, skill/command tables, and repo-structure entries from `README.md`; updated `AGENTS.md`/`CLAUDE.md`, `skills/report/SKILL.md`, and CLI help strings.
  - Deleted `TODO.md` (broker-only).
- Renamed the `brain-style:claude-md` command to `brain-style:agents-md` (`commands/brain-style/claude-md.md` → `agents-md.md`) and reoriented it around `AGENTS.md` as the canonical agent-instructions file.
  - Added a "Canonical File and Symlinks" section: `CLAUDE.md` should be a symlink to `AGENTS.md` (with the check/fix commands), and the same pattern applies to other agents (`GEMINI.md`).
  - Strengthened the Core Principle to stress bare-essentials-only and route all task-specific instruction out to its own file (the routing pattern); added matching Common Mistakes rows.
  - Folded in AGENTS.md best-practices (inspired by community guidance): a ~150-line ceiling, a "Nested Files for Large Repos" section (nearest-file-wins routing for monorepos), a "Never Inline Secrets" rule, a "Sanity-Check the File" validation tip, and a broadened symlink note covering other agents' entrypoints (`GEMINI.md`, `.github/copilot-instructions.md`, `.cursorrules`).
  - Updated the command description, `skills/brain-style/SKILL.md`, and the README slash-command table.
