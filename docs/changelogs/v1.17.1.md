# v1.17.1

Commit range: `d3c04d9..9f62b58` (branch `main`)

## Added

- `LICENSE` — Apache-2.0 full text, copyright 2026 Brian Cefali. `"license": "Apache-2.0"` added to `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`. (`d3c04d9`)
- `tests/test_no_self_ancestor_symlinks.py` — walks `git ls-files -s` for mode `120000` entries, normalizes each target lexically against the symlink's own directory, and fails if it resolves to the link itself or one of its ancestors. Asserts the defect class, not a pinned path. (`110418d`)

## Changed

- `.claude-plugin/marketplace.json` — added top-level `description`, `owner.email`, and an `author` block on the plugin entry. `claude plugin validate .` went from one warning to zero. (`4628186`)
- `.claude-plugin/plugin.json` — added `homepage` and `repository`, matching the values `.codex-plugin/plugin.json` already carried. (`4628186`)
- `.agents/plugins/marketplace.json` — added `description` at both levels; `"path": "./plugins/skill-cefailures"` → `"path": "."`. (`4628186`, `110418d`)

## Removed

- `plugins/skill-cefailures` — a tracked symlink to `..`. Combined with the plugin's `"source": "./"`, it made the plugin root contain itself (`plugins/skill-cefailures/plugins/skill-cefailures/…`), so any symlink-following component scan recursed without termination. The `plugins/` directory went with it. (`110418d`)

## Verification

- `python -m pytest tests/ -v` — 54 passed.
- The new guard was observed **failing** on the pre-fix tree (naming `plugins/skill-cefailures`, correctly ignoring `CLAUDE.md → AGENTS.md`) before the symlink was removed, then passing after.
- `claude plugin validate .` — one warning before `4628186`, zero after.
- Codex tree identity, measured here rather than assumed: fresh clones at `4628186` (old layout) and `110418d` (new) were each installed via `codex plugin add` into isolated `CODEX_HOME`s. The vendored trees differ only by this change's own three edits — the `path` line, the absent `plugins/` symlink, and the new test file. Same skills, same content.
- **Not verified:** that the marketplace now syncs from the claude.ai web app. That is server-side and needs these changes published first.
