# v1.11.0

## Commits since v1.10.1 (`06807f2..HEAD`)

- **feat(commands): add `process-inbox-initiative`** — new orchestrator-only slash
  command (`commands/process-inbox-initiative.md`) that codifies the multi-repo
  initiative recipe previously hand-pasted into org-root inbox docs. Structure:
  scope guard → reuse `process-inbox` steps 1–5 (deferred archive) → capabilities-first
  pass (product-layer commit by orchestrator + early caps-only per-repo dispatch) →
  overview spec + initiatives-ledger seed + archive → orchestrator-spawned researchers
  (affected repos only) and architect (spec-only, `writing-plans` guidance) →
  per-spec dual-review + human gate → parallel-worktree dev (`writing-plans` per task,
  first commit reconciles the step-2 caps commit) → per-repo integration branch.
  Not a SKILL.md (orchestrator-only; must not load into per-repo agent contexts).
- **docs(README):** list `process-inbox-initiative` in the Slash Commands table and
  the `commands/` repo-structure tree.
- **docs(release-notes):** user-facing note for the new command.
