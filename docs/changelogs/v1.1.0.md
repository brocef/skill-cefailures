# v1.1.0

## Added

- `broker` skill with CLI reference and setup docs
- `broker` added to plugin keywords
- One-shot subcommand infrastructure in `broker_cli.py`
- Async Unix domain socket transport (`BrokerServer`) and `BrokerClient` with correlation ID matching
- Client mode in `broker_cli` for connecting to an existing server
- End-to-end integration test for the socket-based broker
- Tests for all one-shot broker CLI subcommands and output formats

## Changed

- README updated for the CLI-based broker (socket architecture)
- `install_broker` uses `--socket` instead of `--storage-dir`
- Help flag test updated for the subcommand-based CLI

## Removed

- MCP broker and install scripts (replaced by CLI subcommands)
