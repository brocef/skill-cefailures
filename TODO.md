# TODO

## MCP Broker

- [ ] Research what Claude Code does when it receives MCP resource update notifications (`notifications/resources/updated`) — does it auto-trigger tool calls, surface to the user, or ignore them?
- [ ] Add MCP resource subscriptions for conversations so agents get proactive push notifications when new messages arrive (instead of polling `read_new_messages`)
